#include <iostream>
#include "regresiones.h"
#include "auxiliares.h"

int main() {
    /*
    RegresorPolinomial rg(3);
    Vector xi(3);
    Vector yi(3);
    xi << 1, 2, 3;
    yi << 4, 5, 6;
    rg.fit(xi, yi);
    rg.mostrar();
    Vector xs(10);
    xs << 2.1, 6.3, 11.4, 3.1, 3.6, 24.9, 2.5, 4.2, 1.4, 4.5;
    Vector sols = rg.predict(xs);
    std::cout << sols << std::endl;
    */
    return 0;
}